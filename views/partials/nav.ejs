 <header>
    <nav style="background-color: #ff5a5f; padding: 1rem 2rem; box-shadow: 0px 2px 8px rgba(0,0,0,0.1);">
      <ul style="display: flex; gap: 20px; margin: 0; padding: 0; list-style: none;">
        <% let homeActive = currentPage === 'Home' ? 'background-color: #e54e52;' : ''; %>
        <% let addHomeActive = currentPage === 'AddHome' ? 'background-color: #e54e52;' : ''; %>
        <li>
          <a href="/" 
            style="color: white; font-weight: bold; text-decoration: none; border-radius: 4px; padding: 0.5rem 1rem; transition: background-color 0.3s;<%= homeActive %>"
          >Home</a>
        </li>
        <li>
          <a href="/host/add-home" 
            style="color: white; font-weight: bold; text-decoration: none; border-radius: 4px; padding: 0.5rem 1rem; transition: background-color 0.3s;<%= addHomeActive %>"
          >Add Home</a>
        </li>
      </ul>
    </nav>
  </header>