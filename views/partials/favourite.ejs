<form action="/favourites" method="POST">
  <input type="hidden" name="id" value="<%= home._id %>" />
  <button type="submit"
    class="flex items-center gap-2 bg-green-500 text-white font-semibold px-4 py-2 rounded-lg shadow-md hover:bg-green-600 transition duration-200">
    <i class="fas fa-heart"></i>
    Add to Favourites
  </button>
</form>